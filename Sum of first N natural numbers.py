def sum_natural_numbers(n):
    return n * (n + 1) // 2

n = 10         # we will change the value of N according to us.
sum_n = sum_natural_numbers(n)
print(f"Sum of first {n} natural numbers is {sum_n}")
